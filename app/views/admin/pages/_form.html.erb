<%= form.inputs do -%>
  <%= form.input :title %>
  <%= form.input :slug, :hint => "Leave blank for an auto-generated slug based on the title." %>
  <%= form.input :body, :hint => "Use Ctrl+E to switch between preview and edit mode.".html_safe %>
<% end -%>
